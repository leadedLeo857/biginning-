#include <iostream>
using namespace std;

int sum (int a, int b) {
    return a + b;}
{
int vichet(int a, int b)
return a - b;
}
int main()
{
    int a, b;
    char s;
    cout << "ÐÐ²ÐµÐ´Ð¸ÑÐµ ÑÐ¸ÑÐ»Ð¾" << endl; 
    cin >> a;
    cout << "ÐÐ²ÐµÐ´Ð¸ÑÐµ Ð·Ð½Ð°Ðº" << endl;
    cin >> s;
    cout << "ÐÐ²ÐµÐ´Ð¸ÑÐµ ÑÐ¸ÑÐ»Ð¾" << endl;
    cin >> b;
    if (s == '+')
    {cout << "Ð¡ÑÐ¼Ð¼Ð°" << endl;
    cout << sum(a,b) << endl;}
    if (s == '-')
    {cout << "Ð Ð°Ð·Ð½Ð¾ÑÑÑ" << endl;
    cout << vichet(a,b) << endl;}
    return 0;
}
