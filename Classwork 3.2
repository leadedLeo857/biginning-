#include <iostream>
using namespace std;

int sum (int a, int b) {
    return a + b;}
{
int vichet(int a, int b)
return a - b;
}
int main()
{
    int n;
    cout << "" << endl; 
    cin >> n;
    int arr[n];
    for (int i=1, i<=n, n++)
    cin >> arr[n];
    int brr[n];
    char x
    cout << "" << endl;
    cin >> a;
    cin >> b;
    cin >> s;
    if (s == '+')
    {
    cout << "" << endl;
    cout << sum(a,b) << endl;}
    if (s == '-')
    {cout << "" << endl;
    cout << vichet(a,b) << endl;}
    return 0;
}
